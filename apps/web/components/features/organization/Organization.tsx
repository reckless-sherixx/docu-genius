import { NavbarButton } from "@/components/shared/NavbarComponents";

export function Organization() {
  return (
    <div>
        <NavbarButton variant="red">Create Organization +</NavbarButton>
    </div>
  )
}
